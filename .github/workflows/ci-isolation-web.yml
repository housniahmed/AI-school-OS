name: CI Isolation Web
on:
  push:
    branches: [feat/v0.6-production-reliability]
permissions:
  contents: read
jobs:
  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Web isolation scheduled"

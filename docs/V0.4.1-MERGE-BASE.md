# V0.4.1 — Baseline alignment

The V0.4.1 feature branch is aligned with the staged V0.3 baseline. The baseline CI workflow is intentionally present on both branches so pull-request validation can execute against the staged release path.

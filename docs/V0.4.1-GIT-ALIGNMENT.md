# V0.4.1 Git alignment

The feature branch is kept explicitly aligned with the staged V0.3 baseline before release validation. This preserves a reviewable release chain and avoids mixing unrelated changes into `main`.

# V0.4 — Production Core

V0.4 treats reliability as a product feature. New modules should not bypass the shared application factory, request correlation, authentication, authorization, tenant isolation, audit trail or validation layers.

## Release gate

A release is not considered production-ready until CI passes for API and web, Prisma schema validation succeeds, integration tests cover authentication and tenant isolation, and deployment/rollback procedures are documented.

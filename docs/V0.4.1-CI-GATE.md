# V0.4.1 CI gate

The release gate is GitHub Actions only: PostgreSQL 16 is provisioned, the versioned Prisma migrations are deployed, the API is built, the authentication/tenant-isolation integration suite is executed, and the web application is built.

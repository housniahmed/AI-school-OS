# V0.4.1 release note

V0.4.1 establishes the production validation foundation: versioned PostgreSQL migrations, CI-backed integration tests, tenant isolation checks, and deployment-oriented health/configuration gates.
